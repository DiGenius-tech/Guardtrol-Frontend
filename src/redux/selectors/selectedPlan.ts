import { RootState } from "../store.js";

// export const selectedPlanState = (state: RootState) => state.selectedPlan;

// export const selectPsConfig = (state: RootState) =>
//   selectedPlanState(state).psConfig;
// export const selectFwConfig = (state: RootState) =>
//   selectedPlanState(state).fwConfig;
// export const selectPlan = (state: RootState) => selectedPlanState(state).plan;
